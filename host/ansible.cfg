[defaults]
roles_path = roles
inventory = inventory/hosts.yml
host_key_checking = False
interpreter_python = /usr/bin/python3
# Pretty YAML task output via the built-in default callback. The old
# community.general 'yaml' stdout callback was removed in community.general v12.
result_format = yaml
nocows = True

[ssh_connection]
pipelining = True
